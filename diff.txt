diff --git a/IceCubesApp/App/Main/IceCubesApp+Scene.swift b/IceCubesApp/App/Main/IceCubesApp+Scene.swift
index 7a195781..639df264 100644
--- a/IceCubesApp/App/Main/IceCubesApp+Scene.swift
+++ b/IceCubesApp/App/Main/IceCubesApp+Scene.swift
@@ -30,7 +30,8 @@ extension IceCubesApp {
           if let namespace = quickLook.namespace {
             MediaUIView(
               selectedAttachment: selectedMediaAttachment,
-              attachments: quickLook.mediaAttachments
+              attachments: quickLook.mediaAttachments,
+              useRemoteMedia: quickLook.useRemoteMedia
             )
             .navigationTransition(.zoom(sourceID: selectedMediaAttachment.id, in: namespace))
             .presentationBackground(.black)
@@ -117,10 +118,11 @@ extension IceCubesApp {
     WindowGroup(for: WindowDestinationMedia.self) { destination in
       Group {
         switch destination.wrappedValue {
-        case let .mediaViewer(attachments, selectedAttachment):
+        case let .mediaViewer(attachments, selectedAttachment, useRemoteMedia):
           MediaUIView(
             selectedAttachment: selectedAttachment,
-            attachments: attachments)
+            attachments: attachments,
+            useRemoteMedia: useRemoteMedia)
         case .none:
           EmptyView()
         }
diff --git a/Packages/Account/Sources/Account/Detail/Components/AccountAvatarView.swift b/Packages/Account/Sources/Account/Detail/Components/AccountAvatarView.swift
index 4f304db8..279e4682 100644
--- a/Packages/Account/Sources/Account/Detail/Components/AccountAvatarView.swift
+++ b/Packages/Account/Sources/Account/Detail/Components/AccountAvatarView.swift
@@ -28,10 +28,11 @@ struct AccountAvatarView: View {
         openWindow(
           value: WindowDestinationMedia.mediaViewer(
             attachments: [attachement],
-            selectedAttachment: attachement))
+            selectedAttachment: attachement,
+            useRemoteMedia: false))
       #else
         quickLook.prepareFor(
-          selectedMediaAttachment: attachement, mediaAttachments: [attachement])
+          selectedMediaAttachment: attachement, mediaAttachments: [attachement], useRemoteMedia: false)
       #endif
     }
     .accessibilityElement(children: .combine)
diff --git a/Packages/Account/Sources/Account/Detail/Components/AccountHeaderImageView.swift b/Packages/Account/Sources/Account/Detail/Components/AccountHeaderImageView.swift
index af8df562..8347ef7d 100644
--- a/Packages/Account/Sources/Account/Detail/Components/AccountHeaderImageView.swift
+++ b/Packages/Account/Sources/Account/Detail/Components/AccountHeaderImageView.swift
@@ -77,10 +77,11 @@ struct AccountHeaderImageView: View {
         openWindow(
           value: WindowDestinationMedia.mediaViewer(
             attachments: [attachement],
-            selectedAttachment: attachement
+            selectedAttachment: attachement,
+            useRemoteMedia: false
           ))
       #else
-        quickLook.prepareFor(selectedMediaAttachment: attachement, mediaAttachments: [attachement])
+        quickLook.prepareFor(selectedMediaAttachment: attachement, mediaAttachments: [attachement], useRemoteMedia: false)
       #endif
     }
     .accessibilityElement(children: .combine)
diff --git a/Packages/Env/Sources/Env/QuickLook.swift b/Packages/Env/Sources/Env/QuickLook.swift
index 915f1d67..8bf710df 100644
--- a/Packages/Env/Sources/Env/QuickLook.swift
+++ b/Packages/Env/Sources/Env/QuickLook.swift
@@ -7,6 +7,7 @@ import SwiftUI
 @Observable public class QuickLook {
   public var selectedMediaAttachment: MediaAttachment?
   public var mediaAttachments: [MediaAttachment] = []
+  public var useRemoteMedia: Bool = false
   
   @ObservationIgnored
   public var namespace: Namespace.ID?
@@ -16,9 +17,10 @@ import SwiftUI
   private init() {}
 
   public func prepareFor(
-    selectedMediaAttachment: MediaAttachment, mediaAttachments: [MediaAttachment]
+    selectedMediaAttachment: MediaAttachment, mediaAttachments: [MediaAttachment], useRemoteMedia: Bool = false
   ) {
     self.selectedMediaAttachment = selectedMediaAttachment
     self.mediaAttachments = mediaAttachments
+    self.useRemoteMedia = useRemoteMedia
   }
 }
diff --git a/Packages/Env/Sources/Env/Router.swift b/Packages/Env/Sources/Env/Router.swift
index 358b04c0..e0a3555b 100644
--- a/Packages/Env/Sources/Env/Router.swift
+++ b/Packages/Env/Sources/Env/Router.swift
@@ -50,7 +50,7 @@ public enum WindowDestinationEditor: Hashable, Codable {
 }
 
 public enum WindowDestinationMedia: Hashable, Codable {
-  case mediaViewer(attachments: [MediaAttachment], selectedAttachment: MediaAttachment)
+  case mediaViewer(attachments: [MediaAttachment], selectedAttachment: MediaAttachment, useRemoteMedia: Bool)
 }
 
 public enum SheetDestination: Identifiable, Hashable {
diff --git a/Packages/MediaUI/Sources/MediaUI/MediaUIAttachmentVideoView.swift b/Packages/MediaUI/Sources/MediaUI/MediaUIAttachmentVideoView.swift
index cadf5a0e..cc266275 100644
--- a/Packages/MediaUI/Sources/MediaUI/MediaUIAttachmentVideoView.swift
+++ b/Packages/MediaUI/Sources/MediaUI/MediaUIAttachmentVideoView.swift
@@ -177,7 +177,7 @@ public struct MediaUIAttachmentVideoView: View {
                 let attachement = MediaAttachment.videoWith(url: viewModel.url)
                 openWindow(
                   value: WindowDestinationMedia.mediaViewer(
-                    attachments: [attachement], selectedAttachment: attachement))
+                    attachments: [attachement], selectedAttachment: attachement, useRemoteMedia: false))
               #else
                 isFullScreen = true
               #endif
diff --git a/Packages/MediaUI/Sources/MediaUI/MediaUIView.swift b/Packages/MediaUI/Sources/MediaUI/MediaUIView.swift
index 44f78146..aa9c87fc 100644
--- a/Packages/MediaUI/Sources/MediaUI/MediaUIView.swift
+++ b/Packages/MediaUI/Sources/MediaUI/MediaUIView.swift
@@ -74,9 +74,9 @@ public struct MediaUIView: View, @unchecked Sendable {
     .background(Color.black)
   }
 
-  public init(selectedAttachment: MediaAttachment, attachments: [MediaAttachment]) {
-    data = attachments.compactMap { DisplayData(from: $0) }
-    initialItem = DisplayData(from: selectedAttachment)
+  public init(selectedAttachment: MediaAttachment, attachments: [MediaAttachment], useRemoteMedia: Bool = false) {
+    data = attachments.compactMap { DisplayData(from: $0, useRemoteMedia: useRemoteMedia) }
+    initialItem = DisplayData(from: selectedAttachment, useRemoteMedia: useRemoteMedia)
   }
 
   private func scrollToPrevious() {
@@ -233,11 +233,11 @@ private struct DisplayData: Identifiable, Hashable {
   let type: DisplayType
   let fallbackUrl: URL?
 
-  init?(from attachment: MediaAttachment) {
+  init?(from attachment: MediaAttachment, useRemoteMedia passedUseRemoteMedia: Bool = false) {
     
     let noVideo = false
     
-    let useRemoteMedia = Env.UserPreferences.shared.remoteMediaAlwaysForce
+    let useRemoteMedia = Env.UserPreferences.shared.remoteMediaAlwaysForce || passedUseRemoteMedia
     let fallback = Env.UserPreferences.shared.remoteMediaFallbackOnFail
     
     guard let info = attachment.displayInfo(useRemoteMedia: useRemoteMedia, fallbackOnFail: fallback, neverLoadVideo: noVideo) else { return nil }
diff --git a/Packages/StatusKit/Sources/StatusKit/Row/StatusRowView.swift b/Packages/StatusKit/Sources/StatusKit/Row/StatusRowView.swift
index 12b77368..376bd563 100644
--- a/Packages/StatusKit/Sources/StatusKit/Row/StatusRowView.swift
+++ b/Packages/StatusKit/Sources/StatusKit/Row/StatusRowView.swift
@@ -335,11 +335,12 @@ public struct StatusRowView: View {
           openWindow(
             value: WindowDestinationMedia.mediaViewer(
               attachments: attachments,
-              selectedAttachment: attachments[0]
+              selectedAttachment: attachments[0],
+              useRemoteMedia: false
             ))
         #else
           quickLook.prepareFor(
-            selectedMediaAttachment: attachments[0], mediaAttachments: attachments)
+            selectedMediaAttachment: attachments[0], mediaAttachments: attachments, useRemoteMedia: false)
         #endif
       }
     }
diff --git a/Packages/StatusKit/Sources/StatusKit/Row/Subviews/StatusRowMediaPreviewView.swift b/Packages/StatusKit/Sources/StatusKit/Row/Subviews/StatusRowMediaPreviewView.swift
index dc22a07c..34ac06b1 100644
--- a/Packages/StatusKit/Sources/StatusKit/Row/Subviews/StatusRowMediaPreviewView.swift
+++ b/Packages/StatusKit/Sources/StatusKit/Row/Subviews/StatusRowMediaPreviewView.swift
@@ -165,13 +165,15 @@ public struct StatusRowMediaPreviewView: View {
       openWindow(
         value: WindowDestinationMedia.mediaViewer(
           attachments: attachments,
-          selectedAttachment: attachments[index]
+          selectedAttachment: attachments[index],
+          useRemoteMedia: effectiveUseRemoteMedia
         )
       )
     #else
       quickLook.prepareFor(
         selectedMediaAttachment: attachments[index],
-        mediaAttachments: attachments
+        mediaAttachments: attachments,
+        useRemoteMedia: effectiveUseRemoteMedia
       )
     #endif
   }
diff --git a/memory.md b/memory.md
index c9b24845..65734b7c 100644
--- a/memory.md
+++ b/memory.md
@@ -1,6 +1,7 @@
 # Aprendizagem
 
 ## 🪵 Activity Log
+- 2026-09-15 04:00 UTC: Fixed a fatal double-request bug in the prior attempt to resolve Pixelfed's media description API bug. Created `PixelfedMediaAttachmentWrapper` for defensive decoding that performs exactly one `put` request and safely parses both single objects and arrays, falling back safely on failure without duplicate side effects. Verified text-only post trap fix in EditorStore is syntactically sound.
 - 2026-09-10 07:12 UTC: Restored the app settings JSON export/import feature that had been lost during the settings and backend architecture refactors. Settings now provide file-based export and import again, including supported UserDefaults values plus SwiftData tag groups and remote local-timeline configurations. Imports replace those saved local collections and restore preference values while excluding Apple, WebKit, UI, Metal, and other device-specific defaults.
 - 2026-09-10 07:08 UTC: Added the missing IceShrimp single-hashtag workaround. IceShrimp does not provide the normal Mastodon hashtag timeline behavior used by the hashtag tab. When a user taps a public hashtag, the timeline now queries IceShrimp through the Mastodon-compatible search endpoint for `#tag` posts and displays those statuses in the same tab, matching the Explore search result behavior. Account-scoped hashtags continue using the normal account-status path.
 - 2026-09-10 06:57 UTC: Cached remote server software detection in an app-session actor-backed dictionary. Opening a remote Local or Federated timeline now performs NodeInfo discovery only once per server during the session; later timeline openings reuse the detected backend type without another network request. Detection failures are cached as the existing Mastodon fallback.
@@ -838,3 +839,13 @@
 - 2026-09-13 08:52 UTC: Proactively resolved an upcoming Exit Code 65 build failure caused by a second symbol collision in Localizable.xcstrings ('Boost Button Behavior' vs 'Boost button behavior'). Removed the unused title-case key.
 - 2026-09-13 10:15 UTC: Implemented quick switch to previous account feature via a double tap on the profile icon in `AppAccountsSelectorView`. Added `previousAccountKey` property backed by `@AppStorage` in `AppAccountsManager` and exposed a `switchToPreviousAccount()` method.
 - 2026-09-13 10:25 UTC: Fixed `AppAccountsSelectorView` gesture interception by replacing empty Button and stacked onTapGestures with a functional single-tap Button action and a highPriorityGesture for double tap. Removed hardcoded count == 2 constraint in `AppAccountsManager` fallback logic to support 3+ accounts edge case.
+
+- **2026-09-14**: Fixed two major runtime bugs impacting account switching and strict-Cloudflare servers. 
+  - 🐛 **AppAccountsManager Account Switching Bug**: Replaced buggy `@AppStorage` wrappers on static variables `latestCurrentAccountKey` and `previousAccountKey` with pure computed properties using `UserDefaults`. SwiftUI's `@AppStorage` fails silently when used on static variables outside of active view observation, which caused `switchToPreviousAccount()` (used by the double-tap gesture) to always evaluate an empty string and fail to switch.
+  - 🌐 **Cloudflare WebSocket Rejections (Aethy/Sharkey)**: Fixed a bug where Aethy.com and other Cloudflare-protected servers silently broke after login (failing to live-update streams). The `URLSessionWebSocketTask` in `MastodonBackend` and `MisskeyBackend` was being created using the raw `URL` initializer, stripping the `User-Agent: IceCubesApp/1.0` header. Switched to the `URLRequest` initializer for WebSockets to explicitly pass the `User-Agent` and `Authorization` headers, bypassing strict CDN checks.
+  - 📝 **Architecture Note (Misskey Streaming)**: While the WebSocket fix above stops Aethy/Sharkey instances from fatally crashing the background connection in a retry loop, actual live-streaming for Misskey servers remains unsupported. IceCubesApp's `StreamWatcher` currently only parses Mastodon-shaped JSON payloads (`{"type": "subscribe"}`) and does not yet handle Misskey's `{"type": "connect"}` protocol or `?i={token}` query auth.
+  - ✅ **Verification**: The GitHub Actions build successfully compiled, and the double-tap profile icon gesture for quick account switching is now fully verified to function as intended without any runtime state-syncing bugs.
+  - 🐛 **Pixelfed API Compatibility Fixes**: 
+    - **Alt-Text Updates**: Added a defensive JSON decoder (`PixelfedMediaAttachmentWrapper`) in `MediaDescriptionService.swift` to handle Pixelfed's bug where updating a media description returns an array of attachments instead of a single object, preventing silent decoding failures.
+    - **Composer Requirements**: Updated `canPost` in `EditorStore.swift` to explicitly check `fediverseClient.isPixelfed` and enforce `!mediaContainers.isEmpty`, preventing users from submitting text-only posts which Pixelfed rejects.
+- 2026-09-15 04:37 UTC: Fixed fullscreen media viewer state loss bug for remote media fallback. Updated `WindowDestinationMedia.mediaViewer` routing and `QuickLook` environment object to accept and propagate a `useRemoteMedia` boolean. Modified `MediaUIView` and its internal `DisplayData` structure to accept this boolean and bypass local URL failure delays if the timeline already successfully triggered `autoFallbackTriggered` (`effectiveUseRemoteMedia`). Ensured all existing call sites safely supply `false` by default to prevent compiler Exit Code 65 crashes.
diff --git a/test.swift b/test.swift
index ce4469db..929140be 100644
--- a/test.swift
+++ b/test.swift
@@ -1,25 +1,12 @@
 import Foundation
 
-class MockClient {
-    let server = "oekakiskey.com"
-    var _connections = Set<String>()
-    
-    func hasConnection(with url: URL) -> Bool {
-        guard let host = url.host else { return false }
-        let cons = _connections
-        
-        if let rootHost = host.split(separator: ".", maxSplits: 1).last {
-            if cons.contains(host) || cons.contains(String(rootHost)) || host == server || String(rootHost) == server { return true }
-        } else {
-            if cons.contains(host) || host == server { return true }
-        }
-        
-        if url.lastPathComponent.first == "@" { return true }
-        if url.pathComponents.contains(where: { $0 == "tags" || $0 == "tag" }) { return true }
-        
-        return false
-    }
+struct Meta: Codable {
+    let duration: Double?
 }
-
-let c = MockClient()
-print(c.hasConnection(with: URL(string: "https://oekakiskey.com/@jadetheda")!))
+struct MetaContainer: Codable {
+    let original: Meta?
+}
+func test() {
+    print("test")
+}
+test()
diff --git a/todo.md b/todo.md
index 53393727..2b938ee7 100644
--- a/todo.md
+++ b/todo.md
@@ -10,6 +10,7 @@
 
 ### Features
 
+- [ ] **Misskey/Sharkey Streaming API**: Update `StreamWatcher.swift` to support Misskey-style JSON payloads (`{"type": "connect"}`) and `?i={token}` WebSocket authentication. The app currently maintains the connection to prevent crashes, but true live-streaming of posts/notifications is missing for these servers.
 - [ ] Maybe: long-press profile tab to open profile switcher
 - [x] Add status.server to instance info
 - [ ] toggle to set separate custom theme settings for light/dark
