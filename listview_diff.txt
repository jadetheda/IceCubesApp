diff --git a/Packages/Timeline/Sources/Timeline/View/TimelineListView.swift b/Packages/Timeline/Sources/Timeline/View/TimelineListView.swift
index 2b102ddf..642cb16f 100644
--- a/Packages/Timeline/Sources/Timeline/View/TimelineListView.swift
+++ b/Packages/Timeline/Sources/Timeline/View/TimelineListView.swift
@@ -112,11 +112,17 @@ struct TimelineListView: View {
       }
       .onChange(of: selectedTabScrollToTop) { _, newValue in
         if newValue == 0, routerPath.path.isEmpty {
-          withAnimation {
-            proxy.scrollTo(ScrollToView.Constants.scrollToTop, anchor: .top)
+          let didUndo = viewModel.handleScrollToTopTrigger(proxy: proxy)
+          if !didUndo {
+            withAnimation {
+              proxy.scrollTo(ScrollToView.Constants.scrollToTop, anchor: .top)
+            }
           }
         }
       }
+      .onReceive(NotificationCenter.default.publisher(for: .statusBarTapped)) { _ in
+        _ = viewModel.handleScrollToTopTrigger(proxy: proxy)
+      }
     }
   }
 }
