# Notes & Lessons

- **Codemagic compile batch (2026-09-08):** The initial cross-spec adapter pass assumed public memberwise initializers for `Emoji`, `MediaAttachment`, and nested media metadata. `public` stored properties do not make Swift's synthesized memberwise initializer public. Add deliberate public initializers for adapter construction, then match parameter names and types to the installed models. Keep `FediverseBackend: Sendable`; an open backend with lock-protected mutable state must explicitly use `@unchecked Sendable` rather than silently dropping the concurrency contract.

- **Codemagic compile failure (2026-09-08):** `FediverseBackend` put `= nil` on a protocol requirement. Swift permits defaults on concrete methods, not protocol requirements. Keep the default on `FediverseClient.get`; make callers of the existential pass `forceVersion` explicitly.

- **Codemagic compile failure (2026-09-08):** `Models/Alias/ServerDate.swift` contained two `public init(date: Date)` declarations after date-formatting work landed together. The Release build failed in `Models` with `invalid redeclaration of 'init(date:)'`. Keep one initializer; use the named `aDay` interval instead of a second literal-`86400` implementation.

- **FAILED ATTEMPT**: Tried fixing Gallery Mode scroll jumping by restoring `scrollToIdAnimated` to `viewModel.lastTopVisibleStatusId` after a 0.5s delay. IT DID NOT WORK.
  - **Reason**: The `GalleryStatusesListView` sets its row IDs to `mediaStatus.id` (which is `attachment.id`), NOT `status.id`. So scrolling to `lastTopVisibleStatusId` did nothing when entering Gallery Mode.
  - **Solution**: We must calculate `lastTopVisibleMediaStatusId` by finding the first visible status with media, and grabbing its `mediaAttachments.first?.id`. When `isGalleryMode` becomes true, we `proxy.scrollTo(lastTopVisibleMediaStatusId)`. When it becomes false, we `proxy.scrollTo(lastTopVisibleStatusId)`.

- **COMPILER CRASH (Exit Code 65) & Bindable Lookup**: In SwiftUI `@Observable` classes, stored properties with observers (`didSet`) but no initial value at their declaration site can fail to participate in `Bindable`'s dynamic member lookup, causing key path resolution failures in compilation (e.g. `Bindable<UserPreferences> has no dynamic member 'hideInteractionButtons'`).
  - **Solution**: Convert these properties into computed properties that get/set their values directly from/to `storage` or backings, explicitly triggering SwiftUI updates using `access(keyPath:)` and `withMutation(keyPath:)`. Ensure you call `withMutation` nested or sequential if other cross-dependent observed properties must also update. Remove their assignments in `init()`.
- **iOS AVPlayer Extension Requirements**: `AVPlayer` silently fails to load remote media if the URL has no file extension or an incorrect extension (like `.gif` for an MP4). Do not try to fix this by adding `"gif"` to a video extensions list—that routes real GIFs to AVPlayer where they will fail permanently. Instead, use `AVURLAsset(url: url, options: ["AVURLAssetOutOfBandMIMETypeKey": "video/mp4"])` to force AVFoundation to treat extensionless (or fake-extension) URLs as video streams.


### Severe Scrolling Lag from `@State` mutations inside `.onAppear`
When tracking image load success states (e.g. `onLoaded` closures triggered by `.onAppear` on images inside lazy structures), doing so via a `@State` property (like `Set<String>`) inside a `List` or `ScrollView` row causes severe performance degradation. Each image load triggers a state mutation, which invalidates the row's layout, forcing a re-render. If multiple images load simultaneously on rapid scroll, the layout engine thrashes.
**Solution:** If the tracked state does not dictate visual changes (e.g. it's only used for canceling background timeout tasks), store the state inside a non-observable private class instance (`private class Tracker { var count = 0 }`) instead of `@State`. This allows pure side-effects without invalidating the SwiftUI view tree.

- **2026-09-10:** Misskey direct messages are not Mastodon statuses. Keep the shared conversation UI and route only the protocol-specific fetch, send, read, and delete operations through `ServerCapabilities.supportsNativeMessaging`; do not make Mastodon execute Misskey API requests.
- **2026-09-10:** Misskey list failures must not be converted into empty or successful-looking UI state. Preserve the server HTTP status, error code, and message in a localized client error, then send list-operation failures through `ErrorService` with popup presentation enabled.
- **2026-09-10:** When adding endpoint cases and nested payload types, verify brace nesting before CI; an extra enum-closing brace in `Conversations.swift` caused a guaranteed parser failure and Exit Code 65.
- **2026-09-10:** Adding a `CodingKey` for an alternate JSON field can disable synthesized `Encodable` when it has no matching stored property. Implement `encode(to:)` explicitly and emit only fields represented by the model.
- **2026-09-10:** Swift view-builder closures cannot contain stored properties, methods, or nested public types. Settings backup declarations must live at the enclosing view type scope, outside button actions and `Section` builders.
- **2026-09-10:** After moving declarations out of a malformed SwiftUI builder, remove the entire duplicate block rather than hiding it with conditional compilation; stale braces and inactive declarations can still confuse parsing and obscure the real scope.
- **2026-09-10:** Types nested in an `@MainActor` SwiftUI view can inherit global-actor isolation. Codable and FileDocument conformances used by system file APIs must be explicitly `nonisolated`; view modifiers also need to attach to a concrete view expression inside the builder.
- **2026-09-10:** Capability-gate every discovery surface, not only the backend request. Unsupported Misskey trending links were still exposed through Explore’s quick-action button even though the backend capability was false.
- **2026-09-10:** When adapting JSON-backed account settings, inspect the encoded keys before mapping them to a native API. `UpdateCredentialsData` emits camelCase keys, while Misskey expects `name`, `description`, `isLocked`, and an ordered `fields` array; swallowing `i/update` errors also made a failed save look like a UI-only problem.
